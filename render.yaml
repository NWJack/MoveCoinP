services:
  - type: web
    name: movecoin
    runtime: static
    buildCommand: "echo static"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
